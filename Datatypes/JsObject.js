// Object properties are written as name:value pairs, separated by commas.

const person = {firstName:"Sirisha",lastName:"Ardhala",age:"23",color:"Pink"};
console.log("The result of the values in :" + person.firstName + person.color);